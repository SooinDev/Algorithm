#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  string a;
  cin >> a;

  string b;
  cin >> b;

  if (a == b) {
    cout << 0 << '\n';
  } else {
    cout << 1550 << '\n';
  }

  return 0;
}
